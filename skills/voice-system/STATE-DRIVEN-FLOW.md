# 语音系统 - 状态驱动开发流程

> **核心原则**: Read state → Act → Update state  
> **不要**: 用"本周/下周"时间规划（这是人类模式，不是 Agent 模式）  
> **状态**: ✅ 流程已建立（2026-03-06 22:46）

---

## 🔄 状态驱动工作流

```
┌─────────────┐
│  读状态文件  │
│ STATE.json  │
└──────┬──────┘
       ↓
┌─────────────┐
│  解析状态    │
│ status/next │
└──────┬──────┘
       ↓
┌─────────────┐
│  决策执行    │
│ blocked?    │
└──────┬──────┘
       ↓
┌─────────────┐
│  更新状态    │
│ 写回文件    │
└─────────────┘
```

---

## 📊 状态定义

### `status` 字段

| 值 | 含义 | 行动 |
|----|------|------|
| `running` | 正常进行中 | 执行 `nextAction` |
| `blocked` | 遇到阻碍 | 尝试解决，3 次失败后求助 |
| `pending` | 等待启动 | 启动最高优先级任务 |
| `done` | 完成 | 归档，清理 |

### `tasks` 结构

```json
{
  "completed": [
    { "id": 1, "desc": "...", "completedAt": "..." }
  ],
  "inProgress": [
    { "id": 2, "desc": "...", "startedAt": "..." }
  ],
  "pending": [
    { "id": 3, "desc": "...", "priority": "high|medium|low" }
  ]
}
```

---

## 🎯 语音系统当前状态

### 阶段 1: 核心重构 ✅ 完成

- [x] 接口定义
- [x] 编排器实现
- [x] 模块化组件（VAD/ASR/TTS/LLM）

### 阶段 2: 功能增强 🔄 进行中

- [ ] 流式 TTS 集成
- [ ] LLM API 接入
- [ ] 配置系统（YAML）

### 阶段 3: 性能优化 ⏳ 待启动

- [ ] GPU 加速
- [ ] 内存优化
- [ ] 并发处理

### 阶段 4: 生产就绪 ⏳ 待启动

- [ ] 错误恢复
- [ ] 完整测试
- [ ] 文档完善

---

## 📋 任务队列管理

### 优先级规则

1. **blocked 任务** → 优先解决（3 次尝试后升级）
2. **inProgress 任务** → 继续执行
3. **pending 任务** → 按优先级启动

### 状态更新时机

- **启动任务**: `pending` → `inProgress`
- **完成任务**: `inProgress` → `completed`
- **遇到阻碍**: `inProgress` → `blocked`
- **解决阻碍**: `blocked` → `inProgress`

---

## 🔧 工具脚本

### 查看状态
```bash
cat skills/voice-system/STATE.json
```

### 更新状态
```bash
node skills/voice-system/update-state.js --status running --next "实现流式 TTS"
```

---

## 💡 经验教训

### ❌ 时间驱动的缺点
- "本周完成 X" → 到周末没完成，上下文丢失
- 需要人类提醒"该做 Y 了"
- 中断后不知道从哪继续

### ✅ 状态驱动的优点
- 随时中断/恢复，状态文件是上下文
- 自己读状态推进，不需要提醒
- 清晰知道下一步做什么

---

## 📁 相关文件

- `STATE.json` - 实时状态（机器可读）
- `TASK-QUEUE.md` - 任务队列（人类可读）
- `STATE-DRIVEN-FLOW.md` - 状态驱动流程说明
- `ARCHITECTURE.md` - 架构文档（包含状态驱动流程章节）
- `DEV-LOG.md` - 开发日志（每次行动记录）

---

## 🎯 已建立的状态驱动基础设施

### 文件清单

| 文件 | 类型 | 用途 | 更新时间 |
|------|------|------|----------|
| `STATE.json` | 机器可读 | 实时状态跟踪 | 2026-03-06 22:46 |
| `TASK-QUEUE.md` | 人类可读 | 任务优先级管理 | 2026-03-06 22:46 |
| `STATE-DRIVEN-FLOW.md` | 文档 | 流程说明 | 2026-03-06 22:46 |
| `ARCHITECTURE.md` | 架构 | 系统设计 + 状态驱动流程 | 2026-03-06 22:46 |

### 核心原则

✅ **状态文件是指令，不是建议**  
✅ **Read state → Act → Update state**  
✅ **不要等人类提醒，自己读状态文件推进**  
✅ **随时中断/恢复，状态不丢失**  

---

**记住**: 状态文件是指令，不是建议。读它，执行它，更新它。
